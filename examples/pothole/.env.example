# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=15432
POSTGRES_DB=civic_os_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# Keycloak Settings (must match your Keycloak setup)
KEYCLOAK_URL=https://auth.civic-os.org
KEYCLOAK_REALM=civic-os-dev
KEYCLOAK_CLIENT_ID=myclient

# MinIO / S3 Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
S3_BUCKET=civic-os-files

# S3 Endpoint Configuration
# S3_ENDPOINT: Internal endpoint for backend services (default: http://minio:9000)
# S3_PUBLIC_ENDPOINT: Public URL for presigned URLs that browsers can access (default: http://localhost:9000)
# Note: S3_PUBLIC_ENDPOINT is required for local MinIO/Docker setups to generate browser-accessible presigned URLs
# For production AWS S3, S3_PUBLIC_ENDPOINT should be empty (presigned URLs will use AWS URLs)

# Notification Worker Configuration (v0.11.0+)
# SMTP Configuration for Email Notifications (vendor-agnostic)
#
# LOCAL DEVELOPMENT (Default):
# By default, docker-compose uses Inbucket (local SMTP server with web UI).
# No configuration needed - just start the stack and view emails at:
#   http://localhost:9100
# Mock data generators use @example.com emails for easy testing.
#
# PRODUCTION SMTP:
# Uncomment and configure the variables below to use a production SMTP server:
#   - AWS SES: email-smtp.us-east-1.amazonaws.com (adjust region)
#   - SendGrid: smtp.sendgrid.net
#   - Mailgun: smtp.mailgun.org
#   - Gmail: smtp.gmail.com (requires App Password)
#   - Custom: your-mail-server.com
#
# AWS SES Setup:
# 1. Verify sender email: https://console.aws.amazon.com/ses/
# 2. Create SMTP credentials (IAM user with SES send permission)
# 3. Use regional SMTP endpoint (e.g., email-smtp.us-east-1.amazonaws.com)
# 4. Port 587 with STARTTLS (recommended) or 465 with SSL
#
# Uncomment these lines for production SMTP:
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USERNAME=AKIAIOSFODNN7EXAMPLE
# SMTP_PASSWORD=BGbLaM1234567890abcdefghijklmnopqrstuvwxyz1234
# SMTP_FROM=noreply@yourdomain.com
# SITE_URL=https://yourdomain.com

# Analytics Configuration (Optional)
# MATOMO_URL: Matomo server URL (e.g., https://analytics.civic-os.org)
# MATOMO_SITE_ID: Unique site ID for this deployment (obtain from Matomo admin)
# MATOMO_ENABLED: Global toggle for analytics (default: true if URL/siteId configured)
# Note: Leave MATOMO_URL empty to disable analytics entirely
# MATOMO_URL=
# MATOMO_SITE_ID=
# MATOMO_ENABLED=true
