# Community Center Reservations Example - Local Docker Development
# Copy to .env and customize if needed

# PostgreSQL connection for PostgREST (uses connection pooling)
PGRST_DB_URI=postgresql://authenticator:mysecretpassword@db:5432/civic_os

# PostgreSQL connection for Sqitch migrations (direct connection, no pooling)
# Format: db:pg://username:password@host:port/database
SQITCH_TARGET=db:pg://postgres:postgres@db:5432/civic_os

# PostgREST API settings (not needed for docker-compose, defined in docker-compose.yml)
# These are for reference or hosted deployments

# Frontend configuration (uses PostgREST on port 3000)
POSTGREST_URL=http://localhost:3000
VITE_POSTGREST_URL=http://localhost:3000

# S3 Configuration (optional - file upload disabled if not configured)
S3_ENDPOINT=
S3_REGION=auto
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET=

# Keycloak Configuration (using shared dev instance)
KEYCLOAK_URL=https://auth.civic-os.org
KEYCLOAK_REALM=civic-os-dev
KEYCLOAK_CLIENT_ID=myclient
