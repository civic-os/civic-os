# Community Center Reservations Example - Local Docker Development
# Copy to .env and customize if needed

# PostgreSQL connection for PostgREST (uses connection pooling)
PGRST_DB_URI=postgresql://authenticator:mysecretpassword@db:5432/civic_os

# PostgreSQL connection for Sqitch migrations (direct connection, no pooling)
# Format: db:pg://username:password@host:port/database
SQITCH_TARGET=db:pg://postgres:postgres@db:5432/civic_os

# PostgREST API settings (not needed for docker-compose, defined in docker-compose.yml)
# These are for reference or hosted deployments

# Frontend configuration (uses PostgREST on port 3000)
POSTGREST_URL=http://localhost:3000
VITE_POSTGREST_URL=http://localhost:3000

# S3 Configuration (optional - file upload disabled if not configured)
S3_ENDPOINT=
S3_REGION=auto
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET=

# Keycloak Configuration (using shared dev instance)
KEYCLOAK_URL=https://auth.civic-os.org
KEYCLOAK_REALM=civic-os-dev
KEYCLOAK_CLIENT_ID=myclient

# Notification Worker Configuration (v0.11.0+)
# SMTP Configuration for Email Notifications (vendor-agnostic)
# Works with AWS SES, SendGrid, Mailgun, Gmail, or any SMTP server
#
# SMTP_HOST: SMTP server hostname
#   - AWS SES: email-smtp.us-east-1.amazonaws.com (adjust region)
#   - SendGrid: smtp.sendgrid.net
#   - Mailgun: smtp.mailgun.org
#   - Gmail: smtp.gmail.com (requires "Allow less secure apps" or App Password)
#   - Custom: your-mail-server.com
#
# SMTP_PORT: SMTP port (587 for STARTTLS, 465 for SSL, 25 for unencrypted)
# SMTP_USERNAME: SMTP authentication username (often email address or API key)
# SMTP_PASSWORD: SMTP authentication password
# SMTP_FROM: Sender email address (must be authorized by SMTP provider)
# SITE_URL: Application URL for notification email links
#
# AWS SES SMTP Setup:
# 1. Verify sender email in SES console: https://console.aws.amazon.com/ses/
# 2. Create SMTP credentials (IAM user with SES send permission)
# 3. Use SMTP endpoint for your region (e.g., email-smtp.us-east-1.amazonaws.com)
# 4. Port 587 with STARTTLS (recommended) or 465 with SSL
#
SMTP_HOST=email-smtp.us-east-1.amazonaws.com
SMTP_PORT=587
SMTP_USERNAME=AKIAIOSFODNN7EXAMPLE
SMTP_PASSWORD=BGbLaM1234567890abcdefghijklmnopqrstuvwxyz1234
SMTP_FROM=noreply@example.com
SITE_URL=http://localhost:4200
